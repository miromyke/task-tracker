{
	# Let's Encrypt notifications (expiry warnings).
	email {$ACME_EMAIL}
}

{$DOMAIN} {
	encode gzip zstd
	reverse_proxy app:8080
}

# Send www → apex so both resolve to one canonical URL.
www.{$DOMAIN} {
	redir https://{$DOMAIN}{uri} permanent
}
